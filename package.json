{
  "name": "library-unit-tests",
  "version": "1.0.0",
  "description": "Unit Test Scripts for Library Management System with AI (Gorse, Ollama)",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.0",
    "typescript": "^5.0.0"
  }
}
